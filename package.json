{
  "name": "open-kb",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "vitest run",
    "check": "node scripts/check-core-purity.mjs && tsc -b && pnpm --filter @open-kb/web exec tsc --noEmit",
    "sweep": "tsx scripts/sweep.ts",
    "overnight": "tsx scripts/overnight.ts",
    "read": "tsx scripts/read.ts"
  },
  "devDependencies": {
    "@openrouter/ai-sdk-provider": "^3.0.0",
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2",
    "vitest": "^3.0.0",
    "tsx": "^4.19.2"
  },
  "dependencies": {
    "ai": "^7.0.48",
    "zod": "^4.4.3"
  }
}
