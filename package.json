{
  "name": "open-kb",
  "version": "0.1.0",
  "description": "Domain in, market map out. Strips products of brand language and searches for what they do — every node and edge cited to a fetched page.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/mo-root/open-kb.git"
  },
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "vitest run",
    "check": "node scripts/check-core-purity.mjs && tsc -b && pnpm --filter @open-kb/web exec tsc --noEmit && tsc -p packages/core/tsconfig.tests.json && tsc -p packages/sweep/tsconfig.tests.json && tsc -p packages/swarm/tsconfig.tests.json",
    "sweep": "tsx scripts/sweep.ts",
    "swarm": "tsx scripts/swarm.ts",
    "overnight": "tsx scripts/overnight.ts",
    "read": "tsx scripts/read.ts",
    "experiment": "tsx scripts/experiment.ts",
    "discover": "tsx scripts/discover.ts",
    "calibrate": "tsx scripts/calibrate-kernel.ts"
  },
  "devDependencies": {
    "@openrouter/ai-sdk-provider": "^3.0.0",
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2",
    "vitest": "^3.0.0",
    "tsx": "^4.19.2"
  },
  "dependencies": {
    "ai": "^7.0.48",
    "zod": "^4.4.3"
  }
}
