
# Optional. Without these, runs are files under runs/ and a crashed run loses
# everything it had not finished. With them, spans are written as they happen,
# so a dead run stays readable and a restarted server can still serve it.
# Schema: scripts/supabase-schema.sql
SUPABASE_URL=
SUPABASE_SECRET_KEY=
