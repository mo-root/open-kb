---
agent: drop-confirm
---
A model read each of these hosts' own front page once and decided, page in hand, that none of
them have any place on a map of this market: `relation: none`, the one verdict that costs a host
its place on the map entirely. That judgement never gets a second opinion — until now.

The market:
  the anchor: {{anchor}} — {{sells}}
  its buyer:  {{buyer}}

For each host below you have exactly what the first pass already established, and nothing more:
what it said the host IS, why it said so, and the roads — the queries — that surfaced it. There is
no fresh page to read; that record already IS the page's content, read once and paid for. The
question is whether it, read again, still argues for nothing.

However loosely connected, does a real placement fit: competitor, substitute, adjacent,
dependency, integration, shaper, buyer, target, covers, lists, discusses — or is the host truly
unrelated whatever way you look at it? A weak connection is still a connection. A host that merely
sells into a nearby part of the buyer's world is `adjacent`, not `none`. A forum where the buyer
occasionally argues about this market is `discusses`, not `none`. A page that writes about the
market even once is `covers`, not `none`. Reach for `none` only when the record in front of you
gives you nothing to hang any of those eleven words on.

Weak is not the same as invented. Eleven words to reach for is not eleven reasons to find one —
the record still has to say the thing, not merely tolerate it being said. This call has no third
opinion coming after it: a rescue stands or falls on this one read, so hold it to the same bar the
first pass was held to, not a lower one because the map would rather keep a host than lose it.

The failure this call exists to prevent is not a wrong label — it is a silent one. A host dropped
here leaves the map for good, on the strength of one model's first read of one page, with no
record that anyone asked twice. Confirming a drop is a real answer and often the right one; the
point is that it is now a checked fact instead of an assumption.

If a placement fits: answer `kind`, `relation`, `what` and `why` exactly as the classify pass
would, stated against the anchor, and back the relation with `spans` — one to three short quotes
copied character-for-character from the what/why/roads given for that host below. Copy, never
invent, and never reach for a page you have not been given: a quote that is not literally in the
record below does not verify, and a placement without a verified quote does not stand. If nothing
fits: answer `relation: "none"`, leave `kind` and `what` as the record already has them, and put
the one line that makes the refusal plain in `why` — that sentence is what gets recorded as the
checked reason.

Answer with `placements`: one row per host, every host answered, `host` copied exactly as given,
`kind`, `relation`, `what`, `why`, and `spans`.

The hosts, one block each — domain, what the first pass said it is, why, and the roads that found
it:

{{hosts}}
