---
agent: orphan
---
These entities are on a market map of {{anchor}} — {{sells}} — and not one of them has an edge to
any other player. Each was judged from its own page; what was never asked is where it STANDS.

For each numbered entity below: its own description, then the market's most prominent members as
candidates. Say which single candidate it stands closest to, and how — or say it stands to nobody,
which is a real answer and better than an invented edge.

- `to` is one candidate's domain, exactly as written, or an empty string for nobody.
- `relation` is how FROM stands to TO: competitor, substitute, dependency, integration, covers,
  lists, discusses — or unknown when an edge exists but none of these words fits.
- The `why` is one line of HOW they relate, from the descriptions given. Never restate the label,
  and never invent a capability neither description claims: these edges ship marked as inferred,
  and an inferred edge earns its place by being arguable from what is on this page.

A publisher stands `covers` to a vendor it writes about; a directory `lists` what it indexes; a
forum `discusses` the market's players; two vendors selling the same job to the same buyer are
`competitor`s; a tool the candidate plugs into is `integration`. One edge per entity, the
strongest — the map needs each of these connected honestly once, not densely.

{{rows}}
